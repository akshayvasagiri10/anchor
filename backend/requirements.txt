# Core API
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20
python-dotenv==1.0.1
pydantic==2.10.4
pydantic-settings==2.7.0

# Generation.
#   openai   — one client for LM Studio, Ollama, Groq, OpenRouter, vLLM.
#   anthropic — 0.121+ is the first release with typed support for
#               output_config (effort) and the server-side `fallbacks` param.
openai==2.48.0
anthropic==0.121.0

# Retrieval
numpy==1.26.4

# Local embeddings on onnxruntime. Same all-MiniLM-L6-v2 vectors as
# sentence-transformers, without torch — ~50MB instead of ~340MB, which is
# the difference between deployable and not.
fastembed==0.7.4

# Document loading
pypdf==5.1.0

# Dev
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1
